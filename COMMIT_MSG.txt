fix(embed): PC・スマホで入力欄を下端に固定し会話ログのみスクロール

- 会話が長くなってもブラウザ下にはみ出さないようにビューポート高さで固定
  - #ai-chat-root を height: 100vh / 100dvh、overflow: hidden に変更
  - html/body に overflow: hidden でページ全体のスクロールを防止
- ログ領域に min-height: 0 を指定し flex 内で正しくスクロールするように修正
- PC は 600px 固定をやめビューポートに合わせて伸縮（min-height: 400px）
